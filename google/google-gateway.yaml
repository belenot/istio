apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: google-gateway
spec:
  selector:
    app: istio-egressgateway
  servers:
  - hosts:
    - '*'
    port:
      number: 80
      name: http
      protocol: HTTP
